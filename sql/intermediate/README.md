# Intermediate Layer

This folder contains SQL models that represent the intermediate logic of the pipeline.
The purpose of the intermediate layer is to perform complex joins, aggregations, and deduplications (e.g., summarizing transaction-level data to the customer level) to prepare the data for the final reporting marts.
