# Staging Layer

This folder contains SQL views for the staging layer. 
The purpose of staging is to extract raw data from source tables and perform basic cleaning, renaming, and type casting before passing it downstream.
