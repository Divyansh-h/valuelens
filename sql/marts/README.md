# Marts Layer

This folder contains SQL models for the marts layer. 
The purpose of the marts layer is to hold the final, business-ready tables and views (like the finalized customer-level RFM scoring table). These are the models that are typically queried directly by dashboards and downstream analytics tools.
